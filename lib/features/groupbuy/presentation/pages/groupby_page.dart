import 'package:flutter/material.dart';

class GroupbyPage extends StatelessWidget {
  const GroupbyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Groupby Page")));
  }
}
